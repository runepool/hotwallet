export interface RuneInfo {
  rune_id: string;
  rune_number: string;
  rune_name: string;
  spaced_rune_name: string;
  symbol: string;
  decimals: number;
  deploy_ts: Date;
  deploy_txid: string;
}

export type RuneID = string;

